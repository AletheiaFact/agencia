USER_AGENT="AletheiaFact.org/Agencia/v1"
OPENAI_API_KEY=""
TAVILY_API_KEY=""
SERPAPI_API_KEY=""  # Deprecated — Tavily is preferred. Remove after migration confirmed.

# Google Fact Check API — use ONE of the following:
#   Option 1 (simplest): API key from Google Cloud Console
GOOGLE_FACTCHECK_API_KEY=""
#   Option 2 (service account): Path to a JSON key file
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account-key.json"

# MongoDB execution store
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DATABASE="agencia"
